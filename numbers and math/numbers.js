const score=234
//console.log(score);//output is only the value

const balance = new Number(100)//output also give the typeof the variable with the values stored in it as well
//console.log(balance);

// console.log(balance.toString());//converts it into string
// console.log(typeof balance);

//console.log(balance.toFixed(3));//used to convert and int to flot by the number of digits fter decimal secified in the parenthesis

const hun=100000000
// console.log(hun.toLocaleString());//converts and add comma in usd standards
// console.log(hun.toLocaleString('en-IN'));//converts in form of indian comma 

